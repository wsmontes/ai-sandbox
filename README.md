# AI Sandbox

Public experiments in small-scale AI-assisted algorithm discovery.

The first experiment is **MicroEvolve-DTS**: evolve search heuristics locally on Apple Silicon to attack the `(7,5)` Difference Triangle Set problem, with an independently verifiable target of scope `<= 111`.
